{
  "scripts": {
    "dev": "next",
    "build": "next build",
    "export": "npm run build && next export",
    "deploy": "npm run export && cd out && now --public -n personal"
  },
  "dependencies": {
    "next": "^6.1.2",
    "react": "^16.5.0",
    "react-dom": "^16.5.0"
  }
}
