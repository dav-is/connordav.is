version: 2.1
orbs:
  cypress: cypress-io/cypress@3
jobs:
  run-cypress:
    executor:
      name: cypress/default
      node-version: "lts"
    steps:
      - cypress/install:
          package-manager: "yarn"
      - cypress/run-tests:
          start-command: "yarn run serve"
          cypress-command: "yarn run cy:run"
workflows:
  use-my-orb:
    jobs:
      - run-cypress:
          name: Run Cypress
